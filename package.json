{
  "name": "recountnyc",
  "version": "1.0.0",
  "description": "Dashboard prioritizing which traffic-volume count locations should be re-collected first, for NYSDOT / NYC DOT planning staff.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node scripts/build-snapshot.js",
    "test": "node --test"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "proj4": "^2.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
